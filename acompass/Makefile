# Generated by makegen....

TARGET = acompass
OBJECTS = acompass.o file.o

CC = gcc
CFLAGS =   $(INCDIR)

LIBS =  
LIBDIR =  
INCDIR =  

INSTALLDIR = 

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $(CFLAGS) -o $(TARGET) $(OBJECTS) $(LIBDIR) $(LIBS)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS)

